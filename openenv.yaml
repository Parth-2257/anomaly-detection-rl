version: "1.0"

env:
  name: anomaly-detection-rl-env

  observation_space:
    type: object

  action_space:
    type: object
    properties:
      is_attack:
        type: boolean
      type:
        type: string
      confidence:
        type: number
      explanation:
        type: string

  endpoints:
    reset: /reset
    step: /step